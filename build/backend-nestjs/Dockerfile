# ==============================================================================
# PAPAS APP - Backend NestJS (Usuario, Login, Perfil, Tareas)
# ==============================================================================

FROM node:20-alpine

WORKDIR /app

# Instalar dependencias del sistema
RUN apk add --no-cache openssl wget

# Exponer puerto
EXPOSE 3001

# Set development environment
ENV NODE_ENV=development
